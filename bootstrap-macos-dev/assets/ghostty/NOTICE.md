# Ghostty shader provenance

- `cursor_warp.glsl` is taken from `sahaj-b/ghostty-cursor-shaders`, commit `4faa83e4b9306750fc8de64b38c6f53c57862db8`.
- `cursor_frozen.glsl` is the customized local shader used by the originating workstation.
- The upstream repository describes the shaders as MIT-licensed.
- Source repository: `https://github.com/sahaj-b/ghostty-cursor-shaders`

Keep this notice with redistributed shader assets.
